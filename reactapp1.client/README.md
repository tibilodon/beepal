# Edit: navigate to edit page, extract productId from params, set product with the matching id

# Data fetch and loading: React-Router-Dom's useLoaderData hook was tested, in order to make less api calls it will not be used.
